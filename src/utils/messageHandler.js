export const createWelcomeMessage = (username) => {
  return `Welcome ${username}! 🎉\nYou are now connected with our sales representative and admin. Feel free to ask any questions!`;
};

export const createNotificationMessage = (username) => {
  return `New conversation started with user: ${username}`;
};